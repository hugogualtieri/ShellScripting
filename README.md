# ShellScripting

+ create_repo.sh:

+ push_master.sh: It's a script that takes one argument (the commit message). You can use this script to push a repository with git.

+ test_generator.sh: It's a script that tests your generator of the generator_solver_maze project
There is the link: https://github.com/hugogualtieri/maze_generator_solver
